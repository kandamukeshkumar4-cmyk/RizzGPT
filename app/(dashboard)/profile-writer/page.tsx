import ProfileWriterPage from '@/components/profile-writer/ProfileWriterPage';

export const metadata = {
  title: 'Profile Writer — RizzGPT',
  description: 'AI-generated dating bios that get more matches',
};

export default function ProfileWriterRoute() {
  return <ProfileWriterPage />;
}
